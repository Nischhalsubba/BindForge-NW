{
  "name": "bindforge-cloudflare-bootstrap",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "opennextjs-cloudflare build",
    "deploy": "opennextjs-cloudflare deploy",
    "upload": "opennextjs-cloudflare upload",
    "preview": "opennextjs-cloudflare preview",
    "check": "npm --prefix web ci && npm --prefix web run check"
  },
  "dependencies": {
    "cloudflare-workspace-proxy": "file:bootstrap/cloudflare-proxy"
  }
}
